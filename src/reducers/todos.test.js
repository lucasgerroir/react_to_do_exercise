it("tests something", () => {
  // what should we test: don't worry about implementation - just write the test descriptions
});

it("should return the initial state", () => {
  // should always return the inital state
});

it('should handle ADD_TODO', () => {
	// if the action is add todo it should break in the addto do statement, list item should be populated
});

it('should handle LIST_TODOS', () => {
	// if the action is list todos it should break in the list todos statement, list item should be populated with the todos
});
