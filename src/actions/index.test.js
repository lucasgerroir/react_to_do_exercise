it("tests something", () => {
  // what should we test: don't worry about implementation - just write the test descriptions
});

it("should create an action to add a todo", () => {
 	// we have to check if the add todo action is calling the right action and passing the right payload through a dispatch
});

it("should create an action to load todos", () => {
	// we have to check if the load todo action is calling the right action and passing the right payload through a dispatch
});
